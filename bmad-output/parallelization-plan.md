# Parallelization Plan — Self-Hosted Voice Language Tutor

> Generated by `/bmad-planning-orchestrator:bmad-parallel-plan`
> Intent: **Create**
> Date: 2026-09-02  •  Max parallel: **3**  •  Waves: **10**

This plan turns the ready-for-dev backlog into conflict-free concurrent **waves**. Each
story gets an isolated git worktree branch with a disjoint file scope. Waves run in order;
inside a wave, stories run concurrently. **This is a plan — your external dev tools create
the worktrees, run the agents, merge, review, and open the PR. This plugin does none of that.**

---

## Inputs

| Input | Source |
|-------|--------|
| Story backlog | `bmad-output/stories/` (all 13 stories at status: ready-for-dev) |
| Sprint status | none — no `sprint-status.yaml` exists yet; all 13 stories were treated as ready-for-dev by default |
| Architecture (semantic boundaries) | none — Quick Flow track has no `architecture.md`; `tech-spec.md#technical-approach` plays that role |
| Dependency graph | `bmad-output/dependency-graph.json` |
| Waves | `bmad-output/waves.json` |

Shared / cross-cutting modules (semantic-conflict sources): none declared beyond the
file-scope conflicts already resolved by wave ordering below (`backend/app/api/router.py`,
`frontend/app.js`, `README.md`).

> **Tooling note:** `build-dependency-graph.py`'s "Blocked by" parser only recognizes a
> bullet starting with the literal text `blocked by`. This project's story files follow
> the official template's `- **Blocked by:** ...` (bold-prefixed) format, which the parser
> does not match — it silently produced 0 `depends_on` edges on the first run. The 10
> missing cross-epic edges (all real, taken directly from each story's Dependency Maps
> section) were added to `dependency-graph.json` by hand before running the wave planner;
> without that fix the wave plan below would have under-sequenced several stories (e.g. it
> would not have stopped 2.1/2.2/2.3/3.1 from being scheduled alongside 1.2, or 4.1 from
> being scheduled before 2.4/3.3 landed).

---

## Waves

### Wave 1 — base branch `main`

| Story | Slug | Worktree branch | Disjoint file scope |
|-------|------|-----------------|---------------------|
| 1.1 | compose-skeleton | `story/1.1-compose-skeleton` | `docker-compose.yml`, `backend/Dockerfile`, `README.md` |

Solo wave — this is the foundation story every other story depends on, directly or
transitively.

**Merge sequence:**
1. Merge `story/1.1-compose-skeleton` -> `integration/wave-1`.
2. Integration review checkpoint.
3. Open PR: `integration/wave-1` -> `main`.
4. Wave 2 branches off the merged `main`.

---

### Wave 2 — base branch `main` (post wave 1)

| Story | Slug | Worktree branch | Disjoint file scope |
|-------|------|-----------------|---------------------|
| 1.2 | backend-skeleton | `story/1.2-backend-skeleton` | `backend/app/main.py`, `backend/app/api/router.py`, `backend/app/api/__init__.py`, `backend/app/pipeline/__init__.py`, `backend/app/scenarios/__init__.py`, `backend/app/state/__init__.py`, `backend/requirements/base.txt`, `backend/tests/test_health.py`, `README.md` |

Solo wave — 1.2 conflicts with 1.1 on `README.md` and is directly Blocked by 1.1, so it
cannot co-run with it; nothing else is unblocked yet (2.1, 2.2, 2.3, 3.1 all depend on 1.2).

**Merge sequence:**
1. Merge `story/1.2-backend-skeleton` -> `integration/wave-2`.
2. Integration review checkpoint.
3. Open PR: `integration/wave-2` -> `main`.
4. Wave 3 branches off the merged `main`.

---

### Wave 3 — base branch `main` (post wave 2)

Run these stories **concurrently**, each in its own isolated worktree:

| Story | Slug | Worktree branch | Disjoint file scope |
|-------|------|-----------------|---------------------|
| 2.1 | stt-integration | `story/2.1-stt-integration` | `backend/app/pipeline/stt.py`, `backend/requirements/stt.txt`, `backend/tests/test_stt.py`, `backend/tests/fixtures/sample_audio.wav` |
| 3.1 | scenario-schema-loader | `story/3.1-scenario-schema-loader` | `backend/app/scenarios/schema.py`, `backend/app/scenarios/loader.py`, `backend/requirements/scenarios.txt`, `backend/tests/test_scenarios.py`, `backend/tests/fixtures/scenarios/**` |

Why these can co-run: pairwise file-disjoint, no shared-module overlap, and both are only
Blocked by 1.2, which landed in wave 2.

**Merge sequence (ascending story id):**
1. Merge `story/2.1-stt-integration` -> `integration/wave-3`.
2. Merge `story/3.1-scenario-schema-loader` -> `integration/wave-3`.
3. Integration review checkpoint on `integration/wave-3`.
4. Open PR: `integration/wave-3` -> `main`.
5. Wave 4 branches off the merged `main`.

---

### Wave 4 — base branch `main` (post wave 3)

Run these stories **concurrently**, each in its own isolated worktree:

| Story | Slug | Worktree branch | Disjoint file scope |
|-------|------|-----------------|---------------------|
| 2.2 | llm-integration | `story/2.2-llm-integration` | `backend/app/pipeline/llm.py`, `backend/requirements/llm.txt`, `backend/tests/test_llm.py` |
| 3.2 | starter-scenarios | `story/3.2-starter-scenarios` | `backend/scenarios/restaurant.yaml`, `backend/scenarios/job_interview.yaml`, `backend/scenarios/small_talk.yaml`, `backend/scenarios/directions.yaml`, `backend/scenarios/shopping.yaml` |

Why these can co-run: pairwise file-disjoint, no shared-module overlap. 2.2 is Blocked by
1.1/1.2 (both landed); 3.2 is Blocked by 3.1 (landed in wave 3).

**Merge sequence (ascending story id):**
1. Merge `story/2.2-llm-integration` -> `integration/wave-4`.
2. Merge `story/3.2-starter-scenarios` -> `integration/wave-4`.
3. Integration review checkpoint on `integration/wave-4`.
4. Open PR: `integration/wave-4` -> `main`.
5. Wave 5 branches off the merged `main`.

---

### Wave 5 — base branch `main` (post wave 4)

Run these stories **concurrently**, each in its own isolated worktree:

| Story | Slug | Worktree branch | Disjoint file scope |
|-------|------|-----------------|---------------------|
| 2.3 | tts-integration | `story/2.3-tts-integration` | `backend/app/pipeline/tts.py`, `backend/requirements/tts.txt`, `backend/tests/test_tts.py` |
| 3.3 | scenario-selection-api | `story/3.3-scenario-selection-api` | `backend/app/api/scenarios.py`, `backend/app/api/session_start.py`, `backend/app/state/session_store.py`, `backend/app/api/router.py`, `backend/tests/test_session_api.py` |

Why these can co-run: pairwise file-disjoint (2.3 never touches `router.py`), no
shared-module overlap. 2.3 is Blocked by 1.2 (landed); 3.3 is Blocked by 3.1/3.2 (both
landed by wave 4).

**Merge sequence (ascending story id):**
1. Merge `story/2.3-tts-integration` -> `integration/wave-5`.
2. Merge `story/3.3-scenario-selection-api` -> `integration/wave-5`.
3. Integration review checkpoint on `integration/wave-5`.
4. Open PR: `integration/wave-5` -> `main`.
5. Wave 6 branches off the merged `main`.

---

### Wave 6 — base branch `main` (post wave 5)

| Story | Slug | Worktree branch | Disjoint file scope |
|-------|------|-----------------|---------------------|
| 2.4 | turn-pipeline | `story/2.4-turn-pipeline` | `backend/app/api/turn.py`, `backend/app/pipeline/turn.py`, `backend/app/api/router.py`, `backend/tests/test_turn.py` |

Solo wave — 2.4 conflicts with 3.3 on `backend/app/api/router.py` and is directly Blocked
by 2.1, 2.2, 2.3, 3.1, and 3.3 (all landed by wave 5); nothing else is unblocked yet (4.1
needs 2.4 itself).

**Merge sequence:**
1. Merge `story/2.4-turn-pipeline` -> `integration/wave-6`.
2. Integration review checkpoint.
3. Open PR: `integration/wave-6` -> `main`.
4. Wave 7 branches off the merged `main`.

---

### Wave 7 — base branch `main` (post wave 6)

| Story | Slug | Worktree branch | Disjoint file scope |
|-------|------|-----------------|---------------------|
| 4.1 | web-ui-core | `story/4.1-web-ui-core` | `frontend/index.html`, `frontend/styles.css`, `frontend/app.js`, `frontend/components/scenario_picker.js`, `frontend/components/recorder.js` |

Solo wave — 4.1 is Blocked by 2.4 and 3.3 (both landed by wave 6); it is also the file
that every later frontend story (`app.js`) must serialize against.

**Merge sequence:**
1. Merge `story/4.1-web-ui-core` -> `integration/wave-7`.
2. Integration review checkpoint.
3. Open PR: `integration/wave-7` -> `main`.
4. Wave 8 branches off the merged `main`.

---

### Wave 8 — base branch `main` (post wave 7)

| Story | Slug | Worktree branch | Disjoint file scope |
|-------|------|-----------------|---------------------|
| 4.2 | live-transcript | `story/4.2-live-transcript` | `frontend/components/transcript.js`, `frontend/app.js` |

Solo wave — 4.2 conflicts with 4.1 on `frontend/app.js` and is directly Blocked by it.

**Merge sequence:**
1. Merge `story/4.2-live-transcript` -> `integration/wave-8`.
2. Integration review checkpoint.
3. Open PR: `integration/wave-8` -> `main`.
4. Wave 9 branches off the merged `main`.

---

### Wave 9 — base branch `main` (post wave 8)

| Story | Slug | Worktree branch | Disjoint file scope |
|-------|------|-----------------|---------------------|
| 5.1 | session-end-restart | `story/5.1-session-end-restart` | `frontend/app.js`, `frontend/components/scenario_picker.js` |

Solo wave — 5.1 conflicts with 4.1/4.2 on `frontend/app.js` and is directly Blocked by
both.

**Merge sequence:**
1. Merge `story/5.1-session-end-restart` -> `integration/wave-9`.
2. Integration review checkpoint.
3. Open PR: `integration/wave-9` -> `main`.
4. Wave 10 branches off the merged `main`.

---

### Wave 10 — base branch `main` (post wave 9)

| Story | Slug | Worktree branch | Disjoint file scope |
|-------|------|-----------------|---------------------|
| 5.2 | error-states | `story/5.2-error-states` | `frontend/app.js`, `frontend/components/recorder.js`, `frontend/styles.css` |

Solo wave — 5.2 conflicts with 4.1/5.1 on `frontend/app.js`/`styles.css` and is directly
Blocked by 5.1. Last story in the backlog.

**Merge sequence:**
1. Merge `story/5.2-error-states` -> `integration/wave-10`.
2. Integration review checkpoint.
3. Open PR: `integration/wave-10` -> `main`.
4. Backlog complete.

---

## Merge order rationale

- Branches in a wave are file-disjoint, so they merge without textual conflict; the
  ascending-id order is for **predictable, reproducible review**, growing the integration
  branch from the most foundational change outward.
- One reviewed PR per wave (`integration/wave-N -> main`) keeps `main` green instead of
  racing N independent PRs.
- Each wave starts from a known-good `main`, so later waves automatically inherit earlier
  waves' changes and their prerequisites hold.
- Only 3 of 10 waves (3, 4, 5) have real concurrency (2 stories each, all in the
  backend/scenario overlap window). The remaining 7 waves are solo by necessity: either
  they're the sole prerequisite everything downstream needs (1.1, 1.2, 2.4, 4.1), or they
  intentionally extend a shared file another story in the same epic already owns
  (`backend/app/api/router.py` for 2.4/3.3; `frontend/app.js` for 4.2/5.1/5.2). This
  backlog is more sequential than parallel by nature — a solo-builder Quick Flow project
  with one dominant frontend file and one dominant router file leaves limited room for
  concurrency past the STT/LLM/TTS-vs-scenario window in waves 3-5.

---

## Deferred / blocked stories

None. All 13 stories had a declared Owned File/Module Scope and were wave-eligible.

---

## Handoff

For each wave, hand your dev tool: the worktree branch names, each story's file scope, and
this merge sequence. The story files themselves remain the authoritative context objects;
their Acceptance Criteria, Dev Notes, and Testing sections are **LOCKED** and must not be
edited during implementation.

> ---
> Part of the **BMAD Planning & Orchestrator** plugin — a Claude Code harness for the **BMAD Method** by the **BMAD Code Organization** (https://github.com/bmad-code-org/BMAD-METHOD). Implements the spirit of `bmad-parallel-plan`. All methodology credit belongs to the BMAD Code Organization.
