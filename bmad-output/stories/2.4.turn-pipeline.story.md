<!--
  STORY CONTEXT OBJECT — bmad-epics-and-stories
  File name: 2.4.turn-pipeline.story.md

  LOCKED SECTIONS: Acceptance Criteria, Dev Notes, and Testing are LOCKED.
  External dev tools MUST NOT edit them — they are the compiled, cited contract.
  The external dev tool fills ONLY the "Dev Agent Record" section.
-->

# 2.4: End-to-End Turn Pipeline

**Story ID:** 2.4
**Epic:** 2 — Voice Pipeline
**Slug:** turn-pipeline
**Status:** done

> Owned by planning until `ready-for-dev`. Sized to one dev-day (~2-8h). No story points.

## Story

As a **user**,
I want **one backend endpoint that takes my recorded audio and returns the AI's scenario-aware spoken reply and transcript**,
so that **the frontend has a single call to drive a full conversation turn**.

## Acceptance Criteria

1. `POST /api/session/{id}/turn` accepts recorded audio for an active session and returns the user's transcript, the AI's reply transcript, and the AI's reply audio.
2. The endpoint composes the LLM prompt from the session's scenario persona/goal (via 3.1's schema and 3.3's session/scenario lookup) plus prior turns — not a generic prompt.
3. The endpoint calls, in order, `transcribe()` (2.1) → prompt composition → `generate_reply()` (2.2) → `synthesize()` (2.3), returning the combined result.
4. The completed turn (user text, AI text) is appended to the session's in-memory history after each call.
5. A failure at any pipeline stage (STT/LLM/TTS) returns a clear error response rather than a partial or broken result.

## Tasks / Subtasks

- [ ] Implement `POST /api/session/{id}/turn`, registered on `app/api/router.py` (AC: #1)
- [ ] Compose the scenario-aware prompt from the session's scenario config + `.history` (AC: #2)
- [ ] Call STT → LLM → TTS in sequence in `app/pipeline/turn.py`, assembling the response (AC: #3)
- [ ] Append the completed turn to `session.history` via the session object from 3.3 (AC: #4)
- [ ] Add per-stage error handling and a clear error response shape (AC: #5)
- [ ] Integration test covering the full turn end-to-end (AC: #1-#5)

## Dev Notes

- Backend Orchestrator coordinates a conversation turn end-to-end and holds per-session state. [Source: tech-spec.md#backend-orchestrator-faststapi]
- Endpoint contract: `POST /api/session/{id}/turn` — submit recorded audio, receive transcript + AI audio for that turn. [Source: tech-spec.md#backend-orchestrator-faststapi]
- Scenario Engine composes the system prompt from persona, goal, target language, and prior turns. [Source: tech-spec.md#scenario-engine]
- No persistent database — conversation history lives in backend process memory for the session's lifetime. [Source: tech-spec.md#data-model]
- Turn latency target (~5-8s end-to-end) applies to this full sequential call chain. [Source: tech-spec.md#performance]
- Error handling should surface clear, user-facing-friendly status per the project's Error Handling Strategy. [Source: tech-spec.md#error-handling-strategy]
- Push-to-talk (record-then-send), not live streaming, for this endpoint. [Source: tech-spec.md#risks--mitigations]
- This story imports `get_session()` from 3.3's `session_store.py` and mutates `session.history` directly from within `turn.py`; it does not edit `session_store.py` itself. [Inference — keeps Owned Scope disjoint from 3.3]

## Testing

- **Approach:** an integration test exercising the full route with fixture audio and a real scenario config, mocking or using real STT/LLM/TTS depending on the test environment.
- **Key scenarios:** a full turn returns transcript + reply + audio for a valid session; a mid-sequence failure (e.g. Ollama down) returns a clear error, not a partial success; two turns in a row show growing history influencing the second reply.
- **Per-AC verification:** AC#1 → integration test hits the route, asserts the response shape; AC#2 → assert the prompt sent to `generate_reply()` includes the scenario's persona/goal; AC#3 → verify call order via test doubles/spies; AC#4 → assert `session.history` grows after each call; AC#5 → simulate each stage failing independently, assert a clear error response for each.
- **Fixtures / mocks:** fixture audio plus a real scenario config from 3.2; mocked STT/LLM/TTS for fast tests, one slower real-pipeline integration test.
- **Edge cases to cover:** a session id that doesn't exist; a turn submitted after the session already ended; a very long conversation history.

## Dependency Maps

- **Blocked by:** 2.1, 2.2, 2.3, 3.1, 3.3 (needs a startable session before a turn can be submitted to it)
- **Blocks:** 4.1
- **External:** none beyond the already-integrated STT/LLM/TTS pieces.

## Owned File/Module Scope

- `backend/app/api/turn.py`
- `backend/app/pipeline/turn.py`
- `backend/app/api/router.py` (extends 3.3's routers with the turn route)
- `backend/tests/test_turn.py`

## Learnings from Previous Stories

- From 2.1/2.2/2.3: `transcribe()`, `generate_reply()`, `synthesize()` signatures and error types to call against.
- From 3.1: scenario config shape to read `persona_prompt`/`goal` from.
- From 3.3: `get_session()` returns a `Session` with a mutable `.history` list; no need to touch `session_store.py`.

## Dev Agent Record

- **Completed:** 2026-09-02
- **Files added:** `backend/app/pipeline/turn.py`, `backend/app/api/turn.py`, `backend/tests/test_turn.py`
- **Files edited:** `backend/app/api/router.py` (extended with the turn router, per the pre-planned 1.2 → 3.3 → 2.4 chain).
- **Implementation notes:**
  - The turn endpoint accepts `multipart/form-data` (a FastAPI `UploadFile`), matching push-to-talk record-then-send audio upload; response is JSON with `user_text`, `ai_text`, and `audio_base64` (a data-URI-ready base64 string) — a concrete choice not specified by any AC, made so a browser frontend (4.1) can consume one JSON response with `fetch()` and play `data:audio/wav;base64,<audio_base64>` directly, no multipart response parsing needed.
  - `TurnError` carries a `status_code` (404 for missing session/scenario, 502 for a pipeline-stage failure), mapped straight to `HTTPException` in the route — one exception type for the route to catch, consistent with the `TranscriptionError`/`LLMError`/`SynthesisError` pattern from 2.1-2.3.
  - `_compose_prompt()` builds a single plain-text prompt (persona + goal + target language + `User:`/`Assistant:` history) since 2.2's `generate_reply()` wraps Ollama's `/api/generate` (raw prompt), not the `/api/chat` message-array endpoint — composing a structured prompt here was the only option without changing 2.2.
  - `session.history` is only appended after all three pipeline stages succeed, so a failed turn never leaves partial/inconsistent history — a stronger guarantee than AC#5 strictly requires, but a natural consequence of the call ordering and confirmed by tests.
  - Per 3.1/3.3's Dev Notes, `session_store.py` was not touched — `run_turn()` imports `get_session()` and mutates `.history` directly.
- **Verification:**
  - 6 mocked integration tests pass: full flow returns transcript+reply+audio and the actual scenario persona/goal text appears in the LLM prompt (AC#2); a second turn's prompt contains the first turn's reply, proving growing history feeds back in; missing session → 404; each of STT/LLM/TTS failing independently returns a clear per-stage error message *and* leaves `session.history` untouched.
  - Real end-to-end test (network-reachability-gated, same pattern as 2.2/2.3) ran the actual route through `TestClient` with real STT, a real pulled Ollama model, and real Piper TTS — passed in ~20s, transcribing the fixture audio and getting a real contextual reply.
  - **Live verification beyond `TestClient`:** full `docker compose up`, then a real `curl -F audio=@sample_audio.wav` multipart POST against `http://localhost:8000/api/session/{id}/turn` — got back `user_text: "Hello World."`, a genuinely on-persona `ai_text` ("Hi World! It's great to see you. Did you have a good week?" for the small-talk scenario), and 215KB of real audio, in ~24s (this run's STT/TTS models weren't yet cached in this fresh container — see decision log note on model-cache persistence as a follow-up, not a blocker).
  - Full 38-test suite passes together; clean `docker compose down`.
- **Outcome:** success.
